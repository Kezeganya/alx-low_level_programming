#include <string.h>
#include <stdio.h>
#include "main.h"

/**
* _strcmp - compares two strings.
* @s1: string to compare.
* @s2: string to compare. 
* Return: returns a negative or positive integer
*/
int _strcmp(char *s1, char *s2)
{
return strcmp(s1, s2);
}
